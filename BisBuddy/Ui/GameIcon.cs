
namespace BisBuddy.Ui
{
    /// <summary>
    /// In-game icons to be used in the UI.
    /// Enum values map to icon IDs.
    /// </summary>
    public enum GameIcon : uint
    {
        // default CollectionStatus.Obtainable icon
        BlueQuestionSquare = 60071,
        // default CollectionStatus.ObtainedPartial icon
        YellowWarningTriangle = 60073,
        // default CollectionStatus.ObtainedComplete icon
        BlueCheck = 60081,
        // default CollectionStatus.Unobtainable icon
        RedCrossSquare = 60861,
        RedCross = 61552,
    }
}
